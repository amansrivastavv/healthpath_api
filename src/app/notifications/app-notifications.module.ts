import { Module } from '@nestjs/common';

/**
 * Placeholder module for App Notifications feature.
 * Will contain push notifications, in-app notifications, and preferences.
 */
@Module({})
export class AppNotificationsModule {}
