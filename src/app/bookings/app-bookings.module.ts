import { Module } from '@nestjs/common';

/**
 * Placeholder module for App Bookings feature.
 * Will contain appointment booking, cancellation, and history.
 */
@Module({})
export class AppBookingsModule {}
